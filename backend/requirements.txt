# Mock-safe backend runtime
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
python-dotenv==1.0.0
Pillow==10.1.0
psutil==5.9.6

# Smoke tests
pytest>=7.4.3
httpx>=0.25.2

# Optional real-mode extensions are intentionally not required for the public
# portfolio demo. Install torch/transformers/pgvector/rembg separately when
# enabling GPU-backed inference or PostgreSQL vector search.
